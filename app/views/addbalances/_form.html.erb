<%= form_for(addbalance) do |f| %>
  <% if addbalance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(addbalance.errors.count, "error") %> prohibited this addbalance from being saved:</h2>

      <ul>
      <% addbalance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
   <%= f.hidden_field :user_id , :value => current_user.id, required: true, class:"form-control" %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount ,required:true%>
  </div>

  <div class="actions">
    <%= f.submit "ADD" ,class:"btn btn-primary" %>
  </div>
<% end %>
